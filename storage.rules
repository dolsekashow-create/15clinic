rules_version = '2';

// No direct client access. Uploads and downloads use short-lived signed URLs
// issued by the server after a permission check.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
