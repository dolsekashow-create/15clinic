# ارفع كل الملفات اللي هنا زي ما هي

المجلد ده مفيهوش أي ملف مخفي — كل اللي شايفه، ارفعه.

## الرفع من المتصفح

1. افتح `github.com/dolsekashow-create/15clinic`
2. اضغط **Add file → Upload files**
3. اسحب **كل** الملفات والمجلدات اللي هنا مرة واحدة
4. اضغط **Commit changes**

## بعدها على Vercel

Settings ← General:

- **Root Directory** = `./`
- **Build & Output Settings** → كل الـOverrides مقفولة (فاضية)
- **Framework Preset** = Next.js

بعدين Deployments ← Redeploy (شيل علامة Use existing Build Cache).

---

## حاجة واحدة بس اعملها في GitHub مباشرة

الملف ده اسمه لازم يبدأ بنقطة، فمينفعش يترفع من المتصفح. اعمله يدوي:

1. في GitHub اضغط **Add file → Create new file**
2. اكتب في خانة الاسم بالظبط: `.gitignore`
3. الصق ده جواه:

```
node_modules/
.next/
.env
.env.local
.env.production
*.log
.DS_Store
```

4. **Commit changes**

**ليه مهم؟** عشان يمنعك مستقبلًا ترفع ملف `.env` فيه مفاتيح Firebase بالغلط. من غيره، أول مرة تحط مفاتيحك في ملف وترفع، المفاتيح تبقى منشورة على GitHub. الموقع هيشتغل من غيره، بس اعمله قبل ما تحط أي مفاتيح حقيقية.

---

## ملف المتغيرات

`ENV-EXAMPLE.txt` ده مجرد قائمة بأسماء المتغيرات اللي محتاجها. **متحطش فيه قيم حقيقية وترفعه.**

القيم الحقيقية بتتحط في Vercel ← Settings ← Environment Variables، مش في ملف.
